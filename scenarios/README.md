# Scenarios

Version 1 supports `static`, `roll`, `pitch`, `yaw`, and `combined` trajectories. They are deterministic mathematical ground-truth trajectories; sensor noise is controlled by a random seed.
